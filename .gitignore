# Dependencies
node_modules/

# Scan outputs (regenerate locally)
Graph.json
Graph.*.json
sqlite/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Logs & env
*.log
npm-debug.log*
.env
.env.*

# OS / editor
.DS_Store
.idea/
*.swp
*.swo

# TypeScript build artifacts (if added later)
dist/
*.tsbuildinfo

# SpOTT-specific architecture preset (local / private monorepo rules)
config/architecture_scan/spott.json

# Local demo / test codebases (scan targets for development only)
jsproject/
project/

# Local ticket files (convention folder; pass path via --ticket=...)
tickets/*
!tickets/README.md
